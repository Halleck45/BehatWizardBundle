hbw.scenario.datasource = function(scenario) {
    this.scenario = scenario;

    this.datas = {
        title: null,
        given: [],
        when: [],
        then: [],
        example: []
    }

    this.addStep= function (step) {

    }
}